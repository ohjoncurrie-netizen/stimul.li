server {
    listen 80;
    listen [::]:80;
    server_name docs.stimul.li;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location = / {
        return 200 "docs bootstrap ready\n";
        add_header Content-Type text/plain;
    }
}
