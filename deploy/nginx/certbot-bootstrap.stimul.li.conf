server {
    listen 80;
    listen [::]:80;
    server_name api.stimul.li;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 "certbot bootstrap ready\n";
        add_header Content-Type text/plain;
    }
}
